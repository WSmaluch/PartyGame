# INSTALL

See [quick start](quick-start.md) and [clean installation](clean-install.md). This package is installed with `scripts/install.sh`, uses `TrustedLanHttp` only after an explicit private-LAN host choice, and requires a recoverable operator token supplied by the operator.
