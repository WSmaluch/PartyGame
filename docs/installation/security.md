# SECURITY

Verify package checksums before installation. Use a 32+ character operator token, retained only in the mode-600 generated configuration file; Admin sends it as a bearer token in memory. HTTP is limited to an explicitly trusted LAN. Use HTTPS outside it, do not expose the Admin endpoint publicly, and share only redacted support bundles.
